import React from 'react'
import '../../../Styles/Cells/Header.css'

const Header = () => {
    return (
        <header className='hero-box-wrapper'>
            <h1 className='hero-box-title'>Demonstrating Data Structure and Array Manipulation with React.</h1>
            <p className='content'>Doing this small project I was using click events, useState, props and other JavaScript methods (filter, map) to demonstrate my skills.</p>
        </header>
    )
}

export default Header